
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.tontinepay.app',
    appName: 'TontinePay',
    webDir: 'out',
    server: {
        androidScheme: 'https'
    }
};

export default config;
